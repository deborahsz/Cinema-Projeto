// Schema do banco de dados do CineScope.
// Os modelos de domínio (User, Favorite, Rating) entram na próxima etapa.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
